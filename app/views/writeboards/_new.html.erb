<%= form_for(writeboard, url: user_writeboards_path(current_user.id), html: {method: "post"} ) do |f| %>
  <p>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
